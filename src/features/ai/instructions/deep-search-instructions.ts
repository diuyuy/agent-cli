export const DEEP_SEARCH_INSTRUCTIONS = `
Generate 5 search queries to help the user explore their topic of interest more deeply. 
Return the queries as an array of strings.

Guidelines:
- Create diverse queries that cover different angles (e.g., background, current status, expert opinions, alternatives, future trends)
- Ensure queries are specific enough to yield useful results
- Avoid redundant or overly similar queries
- Use natural language suitable for search engines
`.trim();
